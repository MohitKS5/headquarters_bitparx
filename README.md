# HeadQuarters_Bitparx
Admin Side Frontend

#### Set Up Instructions
- `npm install`
- `npm start`
- open browser at localhost:4200

#### troubleshooting (if npm doesn't work)
- `install yarn`
- `yarn install`
- `yarn start` or `yarn start --aot`
